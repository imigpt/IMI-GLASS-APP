<?xml version="1.0" encoding="utf-8"?>
<!-- A "Things you can do" suggestion card row (image 1) -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/bg_chat_suggestion_card"
    android:paddingStart="14dp"
    android:paddingEnd="18dp"
    android:paddingTop="16dp"
    android:paddingBottom="16dp"
    android:clickable="true"
    android:focusable="true">

    <ImageView
        android:id="@+id/ivSuggestionIcon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@drawable/bg_chat_suggestion_icon"
        android:padding="10dp"
        android:src="@drawable/ic_note"
        app:tint="#C9C9C9"
        xmlns:app="http://schemas.android.com/apk/res-auto" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="14dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvSuggestionTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Schedule a meeting"
            android:textColor="#E6E6E6"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/tvSuggestionSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="Block time and start meeting"
            android:textColor="#8A8A8A"
            android:textSize="12sp" />
    </LinearLayout>
</LinearLayout>
