<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Filter chip (All / Image / Video / Recording) on the glass media gallery. -->
    <style name="GalleryFilterChip">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:background">@drawable/bg_gm_chip_inactive</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:fontFamily">@font/neue_plak_regular</item>
        <item name="android:gravity">center</item>
        <item name="android:maxLines">1</item>
        <item name="android:textColor">@color/gm_text_primary</item>
        <item name="android:textSize">14sp</item>
    </style>
</resources>
